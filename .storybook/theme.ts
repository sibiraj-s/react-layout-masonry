import { create } from '@storybook/theming/create';

const theme = create({
  base: 'light',
  brandTitle: 'React Masonry',
  brandUrl: 'https://github.com/sibiraj-s/react-masonry',
  brandTarget: '_blank',
});

export default theme;
