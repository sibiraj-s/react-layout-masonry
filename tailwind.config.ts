import { Config } from 'tailwindcss';
const config: Config = {
  content: ['./index.html', './stories/**/*.{js,ts,jsx,tsx}'],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
