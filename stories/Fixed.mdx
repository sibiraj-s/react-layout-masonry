import { Meta } from '@storybook/blocks';

import FixedLayout from './components/FixedLayout';

<Meta title="Fixed Layout" />

# Masonry - Fixed Layout

This is the default, if options are not provided, Masonry will default to 3 columns

```js
<Masonry columns={3} />
```

<FixedLayout />
